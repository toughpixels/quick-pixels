[build]
  command = "hugo --gc --minify"
  context = "production"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.121.1"

[context.production.environment]
  HUGO_VERSION = "0.121.1"
  HUGO_ENV = "production"